import { Context } from "hono";

export const createPost = (c: Context) => {
  return c.json({});
};
export const updatePost = (c: Context) => {
  return c.json({});
};
export const getPost = (c: Context) => {
  return c.json({});
};
